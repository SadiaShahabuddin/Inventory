@model Inventory.Models.SalesType
@{
    var title = "Create Sales Type";
 
}

<div class="card card-default color-palette-box">
    <div class="card-body">
        <form method="post" asp-action="Upsert">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @if (Model.SalesTypeId != 0)
            {
                <input type="hidden" asp-for="SalesTypeId" />
                title = "Edit Sales Type";
            }
            <div class="col-12">
                <h2 class="text-primary">@title</h2>
                <br />
            </div>
            <div class="col-8">
                <div class="form-group row">
                    <div class="col-4">
                        <label asp-for="SalesTypeName"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="SalesTypeName" class="form-control" />
                        <span asp-validation-for="SalesTypeName" class="text-danger"></span>

                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-4">
                        <label asp-for="Description"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="Description" class="form-control" />
                        <span asp-validation-for="Description" class="text-danger"></span>

                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-8 offset-4">
                        @if (Model.SalesTypeId != 0)
                        {
                            <partial name="_EditAndBackToListButton" model="Model.SalesTypeId" />
                        }
                        else
                        {
                            <partial name="_CreateAndBackToListButton" />
                        }
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>