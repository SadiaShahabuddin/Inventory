@model Inventory.Models.ShipmentType
@{
    var title = "Create Shipment Type";
  
}

<div class="card card-default color-palette-box">
    <div class="card-body">
        <form method="post" asp-action="Upsert">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @if (Model.ShipmentTypeId != 0)
            {
                <input type="hidden" asp-for="ShipmentTypeId" />
                title = "Edit Product Type";
            }
            <div class="col-12">
                <h2 class="text-primary">@title</h2>
                <br />
            </div>
            <div class="col-8">
                <div class="form-group row">
                    <div class="col-4">
                        <label asp-for="ShipmentTypeName"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="ShipmentTypeName" class="form-control" />
                        <span asp-validation-for="ShipmentTypeName" class="text-danger"></span>

                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-4">
                        <label asp-for="Description"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="Description" class="form-control" />
                        <span asp-validation-for="Description" class="text-danger"></span>

                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-8 offset-4">
                        @if (Model.ShipmentTypeId != 0)
                        {
                            <partial name="_EditAndBackToListButton" model="Model.ShipmentTypeId" />
                        }
                        else
                        {
                            <partial name="_CreateAndBackToListButton" />
                        }
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>