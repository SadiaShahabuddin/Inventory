@model Inventory.Models.InvoiceType
@{
    var title = "Create Invoice Type";

}

<div class="card card-default color-palette-box">
    <div class="card-body">
        <form method="post" asp-action="Upsert">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @if (Model.InvoiceTypeId != 0)
            {
                <input type="hidden" asp-for="InvoiceTypeId" />
                title = "Edit Invoice Type";
            }
            <div class="col-12">
                <h2 class="text-primary">@title</h2>
                <br />
            </div>
            <div class="col-8">
                <div class="form-group row">
                    <div class="col-4">
                        <label asp-for="InvoiceTypeName"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="InvoiceTypeName" class="form-control" />
                        <span asp-validation-for="InvoiceTypeName" class="text-danger"></span>

                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-4">
                        <label asp-for="Description"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="Description" class="form-control" />
                        <span asp-validation-for="Description" class="text-danger"></span>

                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-8 offset-4">
                        @if (Model.InvoiceTypeId != 0)
                        {
                            <partial name="_EditAndBackToListButton" model="Model.InvoiceTypeId" />
                        }
                        else
                        {
                            <partial name="_CreateAndBackToListButton" />
                        }
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>